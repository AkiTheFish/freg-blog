<!DOCTYPE html>
<%- include("partials/header.ejs") %>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/main.css" />
    <title>blog</title>
  </head>
  <body>
    <div class="create"><a href="/create">create a new post</a></div>
    <% if (posts.length == 0) { %>
    <div class="post">
      <p>no posts 😭😭😭 so lonely...</p>
    </div>
    <% } %> <% posts.forEach(post => { %>
    <div class="post">
      <h1><%= post.title %></h1>
      <a><%= post.date %></a>
      <p><%= post.content %></p>
    </div>
    <% }); %>
  </body>
</html>
